version: 0.2
env:
  shell: bash
  variables:
    NODE_OPTIONS: --max-old-space-size=12000
  debug-session: true
phases:
  build:
    commands:
      - codebuild-breakpoint
      - source ./shared-scripts.sh && _testLinux
